import { MENU_DEMO_KEY } from '../constants/menu';

const menuConfig = {
  key: MENU_DEMO_KEY,
  name: 'Demo',
  subMenu: {
    all: {
      name: 'All Demo'
    },
    test: {
      name: 'Test'
    }
  }
};
export default menuConfig;
